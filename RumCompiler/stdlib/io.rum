// The standard library for RumLang
// Import it using `import rum.std`

import c.stdio;

namespace rum
{

    namespace io
    {
        public void printf(string format, ...){
            c.stdio.printf(format, ...);
        }
    }

}